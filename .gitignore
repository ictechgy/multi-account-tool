# 의존성
node_modules/
.pnp.*
.yarn/

# 빌드 결과물
dist/
build/
*.tsbuildinfo

# 로그
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 에디터 / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# 환경 변수 / 시크릿 — 절대로 커밋하지 않는다
.env
.env.*
!.env.example
.npmrc

# 런타임 / 캐시 / atomic write 임시 파일
.cache/
.tmp/
*.tmp

# AI / IDE 로컬 산출물 (이 저장소에서는 추적하지 않음)
.om*/
.serena/
.claude/
.cursor/
.copilot/
.aider*
.goose/
.crush/
.amp/

# lterm 세션/runtime (ROADMAP #3 통합 시 발생 가능)
.lterm/
.lterm-*

# mat 자체 dev fixture (ROADMAP #2 세션 격리 테스트용)
.mat-sessions/
fixtures/profiles/

# 테스트 커버리지
coverage/
.nyc_output/

# 패키지 매니저 잠금파일은 유지 (npm)
# package-lock.json 은 추적함

# 개인 작업 메모 (핸드오프용, 로컬 전용 — 다음 세션이 fresh context 로 이어받기 위함)
/HANDOFF.md
